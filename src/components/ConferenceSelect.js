import React from 'react';

export default React.createClass({
  render() {
    return (
      <div>
        <select>
          <option>- Choose a Conference -</option>
        </select>
      </div>
    );
  }
});
